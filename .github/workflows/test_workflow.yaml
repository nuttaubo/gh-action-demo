name: Test Github Workflow

on:
  push:
    branches:
      - main

jobs:
  Test-jobs:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Hello
        run: echo "Hello GitHub Actions!"
